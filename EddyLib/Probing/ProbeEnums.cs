namespace EddyLib
{
    public enum fieldType
    {
        vector,

        scalar
    }

    public enum field
    {
        U, p, cp_coeff, epsilon, omega, k, nut, phi, aoa, covid19
        // Todo Zoe
    }
}
